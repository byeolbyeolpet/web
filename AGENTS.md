# This is NOT the Next.js you know

This version (16.x) has breaking changes — APIs, conventions, and file structure may differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.

---

# 라이브러리 작업 전에는 Context7 을 먼저 본다

**라이브러리·프레임워크의 API·설정·규약을 다룰 때는 코드를 쓰기 전에 Context7 MCP 로 최신 문서를 확인한다.** `resolve-library-id` → `query-docs` 순서로 부른다.

기억으로 쓰면 안 되는 이유는 **버전이 앞서 있어서**다. 이 저장소만 해도 Next 16, Tailwind v4, React 19 처럼 학습 시점 이후에 규약이 바뀐 것들이 쌓여 있다.

실제 사고 사례 — 디자인 토큰 작업에서 `--duration-*` 를 Tailwind v4 의 theme 네임스페이스로 착각해 모션 토큰을 정의했는데, 그런 네임스페이스가 없어 유틸리티가 하나도 생성되지 않았다. 빌드 산출 CSS 를 뒤져 0건인 것을 확인하고서야 알았고, 결국 `node_modules/tailwindcss/theme.css` 를 직접 열어 검증해야 했다. Context7 을 먼저 봤으면 한 번에 끝났을 일이다.

- 적용 대상: 새 라이브러리 도입, 설정 파일 작성, API 시그니처·옵션 확인, "이 버전에서 이게 되나?" 류의 판단 전부.
- `node_modules` 의 실제 소스·타입 정의를 읽는 것은 여전히 유효하다. Context7 이 최신 문서를, `node_modules` 가 **설치된 바로 그 버전**을 알려주므로 둘은 대체가 아니라 보완이다.
- 확인한 내용이 판단의 근거가 됐다면 커밋 메시지나 PR 에 남긴다.

---

# 행동 지침 (Behavioral Guidelines)

이 프로젝트는 **속도보다 신중함과 정확성**을 우선한다. 모든 작업은 시니어 엔지니어의 관점에서 한다. `CLAUDE.md`가 "무엇을·왜"라면 이 절은 "어떻게 일하나"다.

## 코드 쓰기 전에 생각한다

- 가정을 명시한다. 불확실하면 질문한다. 마음대로 고르지 않는다.
- 여러 해석이 가능하면 임의 선택하지 말고 사용자에게 선택지를 제시한다.
- 더 단순한 길이 있으면 제안한다. 필요하면 지시에 반대 의견을 낸다.
- 불명확하면 멈추고, 혼란스러운 지점을 밝히고 묻는다.

## 단순함 우선

- 문제를 푸는 최소 코드만 쓴다. 요청받지 않은 기능·"유연성"·"설정 가능성"을 임의로 추가하지 않는다.
- 단일 용도 코드에 복잡한 추상화를 도입하지 않는다.
- 발생 불가능한 시나리오에 대한 과도한 에러 핸들링을 피한다.
- 200줄로 짠 것이 50줄로 되면 다시 짠다.

## 외과수술식 수정

- 필요한 부분만 건드린다. 인접 코드·주석·포맷을 임의로 "개선"하지 않는다. 안 고장난 것을 리팩토링하지 않는다.
- 내 방식과 다르더라도 기존 스타일을 엄격히 따른다.
- 내 변경으로 안 쓰이게 된 import·변수·함수는 즉시 제거한다. 기존의 유휴 코드는 보고만 한다.

## 실재하는 코드를 확인한다

- 수정 전 연관 파일을 모두 읽어 최신 상태와 호출부를 확인한다. 기억·요약에 의존하지 않는다.
- 로컬 코드가 내 가정과 다르면 코드를 신뢰하고 계획을 고친다.
- 데이터 조회 지점 → 타입 정의 → hooks → 호출 컴포넌트 → 렌더 컴포넌트를 하나의 흐름으로 확인한다. 파일 하나만 보고 땜질하지 않는다.
- 좁은 수정을 요청받아도, 그 수정을 정확히 하려면 필요한 연관 코드는 반드시 함께 확인한다.
- **Supabase 규율**: DB에서 필터 가능한 조건은 클라이언트에서 다시 `filter` 하지 말고 쿼리에서 처리한다. `as unknown as` 이중 단언 금지 — select 결과·타입 정의·실제 선택 필드를 일치시킨다. `database.types.ts`상 nullable이 아닌 값에 불필요한 fallback을 넣지 않는다(nullable 여부를 먼저 확인).
- 불필요한 `use client`, 임의 `Map`/`Record` 변환, 중복 display 타입 등 구조적 냄새가 보이면 구현을 멈추고 사용자에게 문제와 대안을 먼저 보고한다.

## 추측을 코드로 쓰지 않는다

라이브러리의 런타임 동작이 예상과 다를 때, 가설을 세워 코드를 고치고 다시 돌리는 것을 반복하지 않는다. **한 번 재고 넘어간다.** 임시 프로브를 만들어 실제 DOM·값·타이밍을 찍어보는 편이 추측 세 번보다 싸다.

실제 사고 — 접근성 스캔의 대비 실패를 잡을 때 motion 의 동작을 세 번 추측해 세 번 틀렸다(`reducedMotion`, `getAnimations`, 인라인 `style.opacity`). DOM 을 직접 찍어보자 5분 만에 끝났다. 한술 더 떠 프로브 자체도 틀렸었다 — `waitForFunction` 에 문자열을 넘기면 "표현식"으로 평가돼 `"() => ..."` 가 함수 객체(truthy)가 되어 즉시 통과한다. **검사 도구가 맞게 도는지부터 확인한다.**

**작업 중 위험을 인지하면 그 자리에서 처리하거나 사용자에게 보고한다.** "알아챘지만 넘어감"은 금지. `Button` 에 `loading` 을 더할 때 `asChild` 가 깨질 수 있음을 인지하고 넘어갔고, 그 결과 마이페이지 전체가 열리지 않았다.

**공용 컴포넌트를 바꾸면 호출부를 전부 확인한다.** 화면 하나 열어보고 끝내지 않는다. 위 `Button` 사고가 정확히 그 경로였다 — `/pet/new` 만 확인하고 `asChild` 를 쓰는 다른 화면을 안 봤다.

## 파일 헤더 주석 (한국어)

- 새 소스 파일 첫 줄에 그 파일의 역할을 설명하는 한 줄 주석을 쓴다. 예: `// 사용자 인증 상태를 관리하는 Context Provider`
- `use client`/`use server` 바로 아래 또는 파일 맨 위에 둔다. 설정 파일은 예외.

## 완료 전 검증

- 코드를 고쳤으면 "완료"라고 말하기 전에 `npm run build`(빌드·타입체크)를 돌린다. 결과(통과/실패/불가 사유)를 구체적으로 보고한다.
- 새 파일 생성, import/export 변경, 타입 변경, hook 로직 변경, Supabase 타입/RPC 변경, query key 변경, 라우트 변경, 공용 컴포넌트 변경은 간단해 보여도 검증한다.
- 단, 단일 파일 내부의 문구·위치·색·여백·className 조정처럼 타입·데이터 흐름·빌드에 영향 없는 변경은 `build`/`lint`를 생략할 수 있다. 생략 시 사유를 명시한다("단순 UI/CSS 조정이라 생략").
- 테스트가 있으면 `npm run test`도 함께 돌린다.

## UI 원칙 (구현 시)

- **모바일 우선**: 모든 스타일은 모바일 뷰를 기본으로 작성한다(터치 WebView가 타깃 — CLAUDE.md의 UX 규칙과 함께 본다).
- **표준 Tailwind 단위**: 4px 스케일(`w-25` 등)을 우선하고 임의값(`[...]`)을 지양한다.
- **사용자 노출 문구 중앙화**: toast·alert·error UI 문구는 상수로 중앙 관리하고, 원본 Supabase/Auth/DB 에러를 사용자에게 직접 노출하지 않는다(`console.error`로만 남긴다). 폼 필드 검증 메시지(Zod/RHF)는 필드 아래에 문장형으로 표시하고 toast로 중복 노출하지 않는다. (구체 상수는 UI 구현 단계에서 확립한다.)
