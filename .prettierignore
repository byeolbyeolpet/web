node_modules
.next
out
coverage
package-lock.json

# Capacitor 네이티브 프로젝트 (assets/public 은 out/ 복사본)
android
ios

# 손으로 정렬한 표·ASCII 트리를 prettier가 흐트러뜨리지 않도록 문서는 제외한다.
*.md

# Supabase 자동 생성 타입 — 생성기 스타일 유지.
src/shared/lib/supabase/database.types.ts
